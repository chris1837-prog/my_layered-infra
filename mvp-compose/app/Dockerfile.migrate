# Dockerfile.migrate
FROM node:20-alpine

WORKDIR /app

# Install full dependencies (needed for node-pg-migrate)
COPY package*.json ./
RUN npm ci

# Copy only migration-related files
COPY pg-migrate-config.js ./
COPY migrations ./migrations

CMD ["npm", "run", "migrate:up"]

